Dependency: tkinter
(to install tikinter. Open command prompt. enter command pip install tkinter.)

The program requires file named "price_list.txt" and "account_log.txt" for its proper functioning. 
Keep these files in the same directory of the script sile "MiniCanteenApp.py"